= link_to social.url do
  - if social.icon.attached? && social.icon.representable?
    = image_tag url_for(social.icon.variant(resize_to_limit: [100, 100])), class: "w-14 h-14 object-fill"
  - else
    = social.name
